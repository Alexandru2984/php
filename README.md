# PHP App Setup

This is a simple PHP and SQLite server project for `php.micutu.com`.

## Structure
- `index.php`: The main entry point, handling frontend form and SQLite logic.
- `database.sqlite`: The SQLite database.

## Deployment
Deployed using Nginx and PHP-FPM, with SSL via Certbot.